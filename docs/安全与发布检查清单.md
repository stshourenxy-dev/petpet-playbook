# 安全与发布检查清单

> **适用对象**：维护者及为本仓库工作的 AI 编程助手。
> **用途**：Release 或打包 / 安全相关变更前，走查客户端安全加固与发布链路。
> **与审查清单的关系**：[`docs/审查清单.md`](审查清单.md) 负责 UI/UX 与工程一致性；本清单负责安全与发布。两者互补，互不替代。
> **与 SECURITY.md 的关系**：SECURITY.md 声明安全边界与漏洞报告渠道；本清单是发布前逐项确认的走查表。
> **边界**：本清单不替代依赖审计（npm audit / pip 审计）、不替代专业安全评估；机器暂不能可靠判定的项目标「待补充」，不伪装成保障。
>
> **维护责任**：项目维护者
> **最近复核清单内容**：2026-08-11
> **最近执行**：暂无记录（自 2026-08-11 起记录）
> **执行频率**：每次 Release 前；打包流程、依赖策略或安全加固变更后

---

## A. 安全加固项（真实加固过，证据可查）

> 证据定位：代码 → `viewer/`；加固记录 → `docs/13-验证台账-20260810.md`（A5-CES 行）；能力状态 → `docs/14-实现状态矩阵.md`。

- [ ] **路径穿越**：宠物包文件读写经 `safePetId` + `isSafeUnderRoot`（relative + realpath 纵深校验）——`viewer/security-utils.cjs:17`（`viewer/main.js:299` 调用）；docs/14「安全-文件路径穿越」commit 14eed9c
- [ ] **压缩包导入（zip slip）**：解压前枚举条目拦截绝对路径 / `../` 段 / Windows 盘符；解压后 realpath 兑底防符号链接逃逸——`viewer/pet-import.cjs`；docs/14「安全-压缩包导入」commit a4d570a（恶意包集成验证通过）
- [ ] **资源炸弹预算（zip 炸弹）**：解压前 `listZipSizes` 读 uncompressed 总量 + 单文件 ≤64MB + 压缩比 ≤100×，超限即拒——`viewer/pet-import.cjs:29-30`（A5-CES-002，真实构造 1027× 与 2001 条目 zip 均解压前拦截，docs/13）
- [ ] **图片解析 fail-closed**：JPEG 按 segment 长度流式扫 SOF（64KB 上限，覆盖长 EXIF）；解析失败 / 未知格式一律拒绝——`viewer/pet-import.cjs:45,222`（A5-CES-003）
- [ ] **JS action 必填与包根约束**：action 必填 frames / frameWidth / frameHeight 对齐 schema required；资源路径 `path.relative` 包根约束（拒 `../`）——`viewer/pet-import.cjs:199,206`（A5-CES-004）
- [ ] **自由文本净化**：name / bubble / label / diary 含不可见或控制字符直接拒绝（不写回、不落盘净化）——`viewer/pet-import.cjs:96` + `pipeline/validate_pet.py` check_text（A5-CES-005）；防提示注入链与间接注入载体（docs/14「安全-自由文本净化」commit 2236b9b）
- [ ] **Electron 加固配置**：`contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`——改 `viewer/main.js` 窗口配置后必须复核这三项未被关闭（SECURITY.md「安全边界」）
- [ ] **依赖支持线**：Electron 43.3.0（33 EOL → 43 支持线，npm audit 通告 30+ → 1）——升级依赖前确认仍在支持线内；docs/14「运行时-依赖支持线」commit 8cd963f

## B. 发布链路（CI 自动 + 人工确认）

- [ ] **CI 全绿**（自动）：ruff / pytest / smoke / vitest / validate_pet / check_docs_sync / check_refs / check_test_counts / boundary（`.github/workflows/ci.yml` + `boundary.yml`）
- [ ] **双端打包**（自动）：macOS dmg + Windows nsis/portable 构建通过；asar 关键模块校验（main.js / pet-import.cjs / security-utils.cjs / preload.cjs 均在包内）
- [ ] **打包三件套**（人工）：发布前确认 asar 内是 `main.js + preload.cjs + dist/index.html + assets/` 全套——docs/07 §6 事故：开发版 index.html 覆盖入口 = 全黑屏
- [ ] **版本一致性**（人工）：tag（vX.Y.Z）= package.json version = 安装包版本号；产物为 draft，人工确认后再发布（A5-CES-006「版本未发」教训：修复合入后必须发版，docs/13）
- [ ] **已发布 tag 不可改写**（纪律）：不再改写已发布 tag（A5-CES-007 历史改写副作用教训，docs/13）
- [ ] **已知限制披露**：rembg 首次运行下载约 170MB 分割模型（无哈希校验）——README / 发布说明如实披露，离线环境需预缓存（SECURITY.md「已知限制」）

## C. 待补充（暂不能可靠执行，不伪装成保障）

- [ ] **依赖审计进 CI**：npm audit / pip 审计目前靠人工或发布前手动执行，未作为 CI 必检步骤（docs/14 V-06 是支持线控制，非自动审计）
- [ ] **供应链 SBOM**：完整依赖 SBOM 与签名校验未建立
- [ ] **数据出境边界**：当前纯本地运行、无网络传输路径；若引入云同步 / 远程能力，需先定义哪些数据可离开本机并补此项
- [ ] **视觉内容安全**：图片 / 动画中的视觉诱导内容无可靠自动化检测手段，靠人工走查
