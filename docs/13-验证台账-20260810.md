# 验证台账 · 2026-08-10

> 对应 commit：`f77fc22` `2b3f079` `746c5fa` `106361d` `07f1a96`（基于 `e9f8212`）
> 背景：外部审计（audit brief + 两轮 AI 复核）发现问题清单后，本轮统一修复并验证。

## 1. 本轮修复内容

| Commit | 类型 | 内容 |
|--------|------|------|
| `f77fc22` | fix(viewer) | 去除 `redshao` 硬编码回退：main.js 5 处 fallback 改为无有效 id 时可见报错/跳过；`petId` 初始值清空（数据正确性：日记/活动不再可能写进示例目录） |
| `2b3f079` | security | 宠物包导入加固：zip slip 防护（解压前枚举条目拦截绝对路径/`../`/盘符 + 解压后 realpath 兜底防符号链接）；`pet:loaded` IPC 补 safePetId 校验 |
| `746c5fa` | refactor(viewer) | 统一行为链调度器 `scheduleTransition`（替代 random 内联 4s 补丁；时长按 frames/fps 估算钳制 2-12s；仅 random/transition 源触发，手动切换不设转移，对齐 state-priority 设计意图）；同步修正 docs/11 过时表述 |
| `106361d` | feat(viewer) | pet.json 数据优先闭环：bubbles/diary/label 运行时真实读取（常量仅兜底）；清理 isOneshot 死导入 |
| `07f1a96` | chore(schema) | schema↔docs 漂移检查机制 `pipeline/check_docs_sync.py` 接入 CI；修复既有漂移：`identity`/`temperament`（V2-A）此前仅存在于代码与 docs，schema 缺失且 `additionalProperties:false` 会拒绝带该字段的包 |

## 2. 测试矩阵

| 项 | 结果 |
|----|------|
| vitest（viewer） | ✅ 62 通过（45 基础 + 10 security-utils + 4 WebP + 3 文本净化） |
| pytest（pipeline） | ✅ 24 通过（21 基础 + 3 文本净化） |
| tsc --noEmit / vite build | ✅ |
| ruff lint（CI 同款） | ✅ |
| node --check（main.js 等） | ✅ |

## 3. 专项验证

### 3.1 zip slip 集成验证（macOS / unzip 分支）

用 python zipfile 构造含 `../../escape-me.txt` 条目的恶意包（zip 命令会 sanitize `../`，真实攻击者用 zipfile）：

```
importPetZip 结果: {"ok":false,"error":"压缩包包含不安全路径条目，已拒绝（zip slip）：../../escape-me.txt"}
os.tmpdir 根下逃逸文件: ✅ 未逃逸
```

条目级拦截生效。Windows 分支（`tar -tf` 枚举）未真机验证——代码路径与 unzip 分支同构，待有 Windows 机器时补测。

### 3.2 纹理泄漏压测（Apple M5 Pro，统一内存架构）

100 次 `redshao` ↔ `pet2`（不同规格素材）切换，进程内存曲线：

| 指标 | 首 | 末 | 峰 | 后半 vs 前半 |
|------|-----|-----|-----|-------------|
| 渲染进程 workingSet | 483MB | 491MB | 503MB | Δ+3MB |
| GPU 进程 workingSet | 102MB | 103MB | 127MB | Δ+1MB |
| 主进程 RSS | 258MB | 238MB | 259MB | Δ−20MB |
| GPU 驱动 In-use system mem（ioreg） | 3398MB | 3231MB | 3409MB | 零增长 |

**结论：无泄漏曲线**——前 ~25 次为缓存建立期，之后全程平台期。`Texture.destroy()`（连 TextureSource）在 100 次尺度上有效，Assets cache 未造成可测泄漏。

限制：进程级 workingSet + ioreg GPU 驱动统计（统一内存架构下最接近「显存」的可观测指标），非显存仪表直接读数；样本 2 宠物 × 100 次 × 单机，3+ 宠物/高分屏待后续补充。

### 3.3 transitions 行为链实测

自然运行 10 分钟，hook `pet:action:notify` 收集 175 次动作切换：

- `sleep → stretch → idle` 链完整、按权重（sleep: stretch 80 / idle 20）正确转移，反复出现（10 分钟内 ≥20 次完整链）
- 切换间隔实测 2401-2403ms vs 理论 2400ms（12 帧 @ 5fps）——**误差 ±2ms**，frames/fps 即一轮精确时长，视觉无感
- 零跳变、零死锁（无 <2400ms 的异常打断）

**结论：时长估算方案满足产品预期，无需改用 AnimatedSprite.onComplete。**

### 3.4 scheduleTransition 边界测试（scripts/boundary-test.cjs，F-004）

| 场景 | 结果 |
|------|------|
| A 手动打断（menu 源不挂转移定时器） | ✅ 手动切 sleep 后 4.5s 无意外转移 |
| B reminder 打断（reminder 源播 wiggle） | ✅ wiggle 播放后无意外转移 |
| C 多宠切换瞬间（loadPet 清除旧定时器） | ✅ 切换后 5s 无旧宠物动作残留 |
| D loop 长时稳定性 | ✅ 20s 观察无 <1.8s 快速抖动 |

**测试资产（已入库）**：`viewer/scripts/memtest.cjs`（纹理泄漏压测：自动发现宠物、参数化切换次数/间隔、平台期判定）；`viewer/scripts/boundary-test.cjs`（F-004 四场景边界测试）。

## 4. 已知限制（未验证项）

1. Windows 真机：zip slip tar 分支、DPI/多屏矩阵（无 Win 机器，代码路径同构）
2. 压测样本窄：3+ 宠物、高分屏、跨天长时运行未覆盖
3. 主题换肤入口：隐藏保留（P2，上游 P1-5 已删 switchTheme）

## 5. 验证环境

- macOS（Apple M5 Pro，16GB 统一内存，Metal 4）
- Node 22 / Electron 43.3.0 / PixiJS 8.6 / vitest 4 / Python 3.12（2026-08-10 升级：33 EOL → 43 支持线，npm audit 通告 30+ → 1）

## 6. 工程经验：CI 假绿（2026-08-10，v1.2.1 前）

**现象**：boundary-test.cjs 断言失败时仍 `app.quit()`（exit 0）——CI workflow 只看 exit code，失败也判绿。首次接入 CI 时未暴露（本地手动跑人眼能看到 FAIL）。

**铁律**：**测试的失败路径本身必须被测试**——新 workflow 首跑必须做负向验证（注入一个必然失败的用例，确认 CI 真的变红），再还原。

**同类排查**：isSafeUnderRoot 的 macOS `/var→/private/var` 符号链接缺陷也是靠测试抓出的——被测试覆盖前「没出过问题」，覆盖后「被证明正确」。

**验证**：真实 PASS exit 0 / 注入 FAIL exit 1（0c9a77c）。

## 7. AI 工具全量审计处理记录（2026-08-10 追加）

| 项 | 处理 | commit |
|----|------|--------|
| 隐私：remoteedu@gmail.com 泄露（d8231d2 提交者邮箱） | git-filter-repo 历史改写 → noreply；后续 42 commit hash 变更 + docs 引用同步 + 3 tag 重建 + force push；远程可见邮箱仅剩 noreply | 33800f7（改写后主链） |
| V-10 自由文本净化 | validate_pet.py + pet-import.cjs 双端：零宽/BiDi/控制字符剥离 + maxLength（name50/bubble100/label30/diary200）；Python+3/JS+3 用例 | dc82d5b + bf962a8 |
| V-06 Electron EOL | 33.4.11 → 43.3.0（npm audit 30+ → 1 high/nanoid）；electron-builder 25→26；本地真 Electron 冒烟 4 场景 | a1f7d15 |
| CI 独有失败修复 | 裸 pytest 不加仓库根到 sys.path → `from pipeline.validate_pet` 改直接模块名（python -m pytest 本地盲区） | bf962a8 |
| 验证盲区教训 | **本地验证必须用与 CI 相同的命令**（裸 pytest vs python -m pytest 的 sys.path 差异）——3 种本地环境全部用 python -m pytest，掩盖了 CI 独有失败 | — |
