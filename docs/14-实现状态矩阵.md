# 实现状态矩阵（Implementation Status Matrix）

> **用途**：能力 → Schema / Runtime / Viewer 三层实现状态的唯一事实源。
> 任何 AI 审计、文档阅读、代码评审**先读本表**，再读代码——避免「文档说实现了 vs 代码只实现一半」的反复误判。
> **维护纪律**：代码或文档变更时同步更新本表；状态与代码冲突时以代码为准并修正本表。

状态图例：✅ 完成（有测试/实测证据）｜🟡 部分实现或演进中｜❌ 未实现｜**设计** = 仅契约/方案，无实现

| 能力 | Schema | Runtime（渲染层） | Viewer（主进程） | 状态 | 证据（文件:位置） |
|------|:---:|:---:|:---:|------|------|
| bubbles 气泡文案池 | ✅ v3 | ✅ `bubblePool` | — | **完成** | viewer/src/main.ts；docs/08 §2.2 |
| label 动作菜单名 | ✅ v3 | — | ✅ `actionMenuLabel` | **完成** | viewer/main.js；docs/08 §2.2 |
| diary 日记文案 | ✅ v3 | ✅ `resolveActionDiary` | ✅ `appendActivity` | **完成** | viewer/src/main.ts |
| transitions 转移链 | ✅ v3 | ✅ `scheduleTransition`（时长按 frames/fps，钳制 2-12s，仅 random/transition 源） | — | **完成**（数据驱动转移链，10 分钟实测 175 事件链路正确） | viewer/src/main.ts；验证台账 §3.3 |
| temperament 气质调制 | ✅ V2-A | ✅ `effectiveWeight` 调制随机权重（±30%，钳制 0.7-1.3） | — | **完成** | viewer/src/temperament.ts；main.ts:640 |
| identity 身份事实 | ✅ V2-A（2026-08-10 补入 schema） | 🟡 类型就绪，未消费 | — | **就绪 / 待消费**（docs/08 注明「供未来行为映射与展示」） | viewer/src/main.ts:38 |
| 长期记忆协议 | **设计** | ❌ | ❌ | **规划** | docs/12-优化路线图.md |
| 安全-文件路径穿越 | — | — | ✅ `safePetId` + `isSafeUnderRoot`（relative + realpath） | **完成** | viewer/main.js:343-447；SECURITY.md |
| 安全-压缩包导入（zip slip） | — | — | ✅ 解压前条目枚举拦截 + 解压后 realpath 兜底（2026-08-10 加固） | **完成**（恶意包集成验证通过） | viewer/pet-import.cjs；验证台账 §3.1 |
| 多宠切换纹理释放 | — | ✅ `Texture.destroy` 连 source | — | **完成**（100 次切换压测平台期，GPU 侧零增长） | 验证台账 §3.2 |
| 显式行为状态机（idle/play/complete 状态枚举） | **设计** | 🟡 当前为「计时器 + 权重转移」的数据驱动链，非显式状态枚举 | — | **演进中**（当前实现满足 docs/08 §2.4 承诺，复杂状态机为后续方向） | docs/08 §2.4；docs/12 |

---

## 说明

1. **三层含义**：Schema = 契约字段存在；Runtime = 渲染进程消费；Viewer = 主进程消费（菜单/安全/导入等）。
2. **「完成」的定义**：字段进契约 + 被消费 + 有测试或实测证据（见 13-验证台账-20260810.md）。
3. **审计速查**：先看本表找目标能力的「状态」列，再按「证据」列定位代码；如发现本表与代码不符，以代码为准并修正本表。
