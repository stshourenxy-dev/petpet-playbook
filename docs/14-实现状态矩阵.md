# 实现状态矩阵（Implementation Status Matrix）

> **用途**：能力 → Schema / Runtime / Viewer 三层实现状态的唯一事实源。
> 任何 AI 审计、文档阅读、代码评审**先读本表**，再读代码——避免「文档说实现了 vs 代码只实现一半」的反复误判。
> **维护纪律**：代码或文档变更时同步更新本表；状态与代码冲突时以代码为准并修正本表。
> **可审计性**：每行附「验证 commit」——证据链可追溯到具体提交；本表自身也是一份可审计资产（Last verified commit = 本表最近一次核对的提交，见文末）。

状态图例：✅ 完成（有测试/实测证据）｜🟡 部分实现或演进中｜❌ 未实现｜**设计** = 仅契约/方案，无实现

| 能力 | Schema | Runtime（渲染层） | Viewer（主进程） | 状态 | 证据（文件:位置） | 验证 commit |
|------|:---:|:---:|:---:|------|------|------|
| bubbles 气泡文案池 | ✅ v3 | ✅ `bubblePool` | — | **完成** | viewer/src/main.ts；docs/08 §2.2 | 106361d |
| label 动作菜单名 | ✅ v3 | — | ✅ `actionMenuLabel` | **完成** | viewer/main.js；docs/08 §2.2 | 106361d |
| diary 日记文案 | ✅ v3 | ✅ `resolveActionDiary` | ✅ `appendActivity` | **完成** | viewer/src/main.ts | 106361d |
| transitions 转移链 | ✅ v3 | ✅ `scheduleTransition`（时长按 frames/fps，钳制 2-12s，仅 random/transition 源） | — | **完成**（数据驱动转移链；10 分钟实测 175 事件链路正确；边界测试 4/4） | viewer/src/main.ts；台账 §3.3/§3.4 | 746c5fa + 56722b7 |
| temperament 气质调制 | ✅ V2-A | ✅ `effectiveWeight` 调制随机权重（±30%，钳制 0.7-1.3） | — | **完成** | viewer/src/temperament.ts；main.ts:640 | cf4ce50 |
| identity 身份事实 | ✅ V2-A（07f1a96 补入 schema） | 🟡 类型就绪，未消费 | — | **就绪 / 待消费**（docs/08 注明「供未来行为映射与展示」） | viewer/src/main.ts:38 | 07f1a96 |
| 长期记忆协议 | **设计** | ❌ | ❌ | **规划** | docs/12-优化路线图.md | — |
| 安全-文件路径穿越 | — | — | ✅ `safePetId` + `isSafeUnderRoot`（relative + realpath） | **完成** | viewer/main.js:343-447；SECURITY.md | d888529 |
| 安全-压缩包导入（zip slip） | — | — | ✅ 解压前条目枚举拦截 + 解压后 realpath 兜底（2b3f079 加固） | **完成**（恶意包集成验证通过） | viewer/pet-import.cjs；台账 §3.1 | 2b3f079 |
| 多宠切换纹理释放 | — | ✅ `Texture.destroy` 连 source | — | **完成**（100 次切换压测平台期，GPU 侧零增长） | 台账 §3.2；scripts/memtest.cjs | a34bc92 + 56722b7 |
| 显式行为状态机（idle/play/complete 状态枚举） | **设计** | 🟡 当前为「计时器 + 权重转移」的数据驱动链，非显式状态枚举 | — | **演进中**（当前实现满足 docs/08 §2.4 承诺，复杂状态机为后续方向） | docs/08 §2.4；docs/12 | 746c5fa |
| 安全-自由文本净化（V-10） | ✅ v3（validate_pet.py 的 check_text） | ✅ `cleanText`（pet-import.cjs） | — | **完成**（零宽/BiDi/控制字符剥离 + maxLength：name 50/bubble 100/label 30/diary 200；防提示注入链与间接注入载体；Python +3 / JS +3 用例） | pipeline/validate_pet.py；viewer/pet-import.cjs | bf962a8 |
| 运行时-依赖支持线（V-06） | — | — | ✅ Electron 43.3.0（33 EOL → 43 支持线） | **完成**（npm audit 通告 30+ → 1；本地真 Electron 冒烟 4 场景过） | 台账 §6；electron 依赖声明 | a1f7d15 |

---

## 说明

1. **三层含义**：Schema = 契约字段存在；Runtime = 渲染进程消费；Viewer = 主进程消费（菜单/安全/导入等）。
2. **「完成」的定义**：字段进契约 + 被消费 + 有测试或实测证据（见 13-验证台账-20260810.md）。
3. **审计速查**：先看本表找目标能力的「状态」列，再按「证据」列定位代码；如发现本表与代码不符，以代码为准并修正本表。
4. **验证 commit 列**：该能力最近一次实现/验证对应的提交 hash，用于追溯证据链；多 commit 时列出全部相关。
5. **Last verified commit**：`56722b7`（2026-08-10 测试资产入库，本表最后一次全面核对；此后任何代码变更应先更新本表再合入）。
