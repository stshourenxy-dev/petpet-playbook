# Contributing · 贡献指南

感谢你愿意为这个 Playbook 添砖加瓦。这个仓库的核心是**方法论的诚实**——"真实数据、真实踩坑、不做无证据的宣称"。请带着这个标准来。

## 提交规范

- **Commit message**：`<type>: <中文摘要>`（type: fix / feat / docs / test / chore），正文写清"改了什么 + 为什么"。
- **单次提交**：一个主题一个提交，不要混改。
- **提交信息中性**：提交信息、注释和审计记录不得包含具体 AI 工具、厂商或模型名称；需要提及审计或生成来源时用「AI 工具」「AI 助手」等通用表述。本地 commit-msg hook 提交前拦截；CI 有同名检测项（配置 Secret 后生效）。
- **改动前先看**：`docs/07-踩坑实录.md`——很多坑已经踩过，别重复发明。

## 提 PR 前自查（对抗性审查清单）

发布或大改动前，用 `docs/审查清单.md` 的六个维度走查一遍。核心是**陌生人视角**：

1. **身份假设**：你的改动是不是只对"作者的机器/宠物"成立？换成别人的宠物、别人的路径还能跑吗？
2. **错误路径**：输入错误、文件缺失、加载失败时，是静默还是可见报错？有没有把解析结果回显给用户？
3. **迭代残留**：改 A 忘 B 了吗？删功能时底层 IPC/写盘路径删干净了吗？grep 一下有没有无调用者的代码。
4. **边界与量纲**：最小窗口会裁切吗？定时器 × 副作用 × 存储上限的算术算过了吗？
5. **可访问性**：对比度、键盘、屏幕阅读器。
6. **文档一致性**：README/docs 的默认值、路径、承诺和代码对得上吗？**写文档时查代码，不凭记忆。**

## PR 检查（勾选提示，非门禁）

- [ ] 变更涉及 UI / 运行时 / 打包 → 已执行 `docs/审查清单.md`（附基线 commit 与结果）
- [ ] 变更涉及安全 / 打包 / 发布链路 → 已执行 `docs/安全与发布检查清单.md`
- [ ] 不涉及上述范围，或已记录不适用原因

> 勾选的作用是**留下解释与执行证据**，不是质量证明；不设机器强制，未来配置 branch protection / required checks 后再谈门禁。

## 测试要求

- **管线脚本（pipeline/）**：改完必须过 `tests/`（`pytest tests/ -v`）和 `ruff check pipeline/ tests/`。
- **viewer**：改完必须过 `npm test`（vitest）和 `npm run build`（含 tsc 类型检查）。
- 新增边界情况时**加用例**——测试的价值是"能抓到 bug"，不是覆盖率数字。注意：夹具别照着实现写（那会把实现的 bug 一起固化进测试）。

## 示例素材上限（重要）

仓库的 `examples/` 只放**最小可运行示例**，不放完整素材包：

- **单个精灵表 ≤ 2MB**，优先 WebP 无损（同内容比 PNG 小 30-40%）。
- **单个示例宠物包 ≤ 10MB**（当前 `redshao-demo` 约 4MB 是标杆）。
- **完整素材（大体积视频/全量精灵表）不要提交**——只放链接或说明（见 docs/11 §3.2 收录页思路）。
- 提交前检查：`du -sh examples/*/`，超过上限就压缩或瘦身。

git 历史不可逆——素材一旦提交就永远留在仓库里，体积问题会一直跟着 fork 的人。

## 新增文档

- 写进 `docs/` 的每个数字都要能说出处（实验数据 / 实测 / 诚实标注"经验值未验证"）。
- 新增章节时更新 `README.md` 的文档导航。

## Issue 模板要点

- 环境：操作系统 / Python 版本 / Node 版本
- 复现步骤：输入什么、执行什么命令、期望 vs 实际
- 是否按 docs 操作：如果照着文档做失败了，请指出文档与代码不符的地方（这是最值钱的反馈）
