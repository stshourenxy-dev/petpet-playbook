# 第二轮审查 · 复验 + 未覆盖面

> 第一轮（`UIUX-对抗性审查.md`）只审了 `viewer/` 的 UI/UX。本轮做两件事：
> **① 对抗性复验**已落地的 7 个修复提交，找"看起来修了但没修到位"的；
> **② 审第一轮没碰过的面**：`pipeline/` 脚本、测试有效性、CI 覆盖、Electron 安全面、文档覆盖率、仓库工程化。
> 基线：`1d49fa3`（第一轮）→ `9b0059b`（本轮）· 日期：2026-08-07

---

## 0. 摘要

第一轮 37 项里绝大多数已真实修复且质量不错——`reminder.ts` 的正则修复配了 6 个表驱动用例、幽灵换肤是**删干净**而不是糊过去、提醒落盘 + 系统通知兜底、多宠切换补上了托盘子菜单。

本轮结论：

| 类别 | 数量 | 说明 |
|------|------|------|
| **复验未到位** | 3 | 修复方向对，但盒模型/忽略规则导致实际没生效 |
| **A 管线脚本** | 9 | `pipeline/` 从未被审过，含 1 个危险 CLI 行为 |
| **B 测试有效性** | 4 | 含"测试夹具恰好绕开了被测 bug"和 **CI 从不跑 viewer 单测** |
| **C 文档覆盖** | 2 | docs/06 的承诺与覆盖范围不符；全仓库 .md 互链数为 0 |
| **D 安全面** | 4 | 宠物包是设计上要分享的 → 这是真实攻击面，不是理论 |
| **E 工程化** | 5 | 无 lint、依赖无上界、授权边界、CI 不覆盖唯一实测平台 |

---

## 复验：3 处修复方向对但没生效

### R-1 · P1-6 布局溢出：三个浮层仍然溢出，因为 `.panel` 还是 content-box

修复用了 `calc(100% - 16px)`，这个写法**假设了 border-box**，但 `.panel` 至今没有 `box-sizing`，全仓库唯一的 `box-sizing: border-box` 在 `#reminder-input`（`style.css:203`，第一轮之前就有）。

于是 `max-width` 约束的是**内容盒**，padding 和 border 加在外面：

| 元素 | 当前规则 | padding+border | 实际渲染宽 | 结果 |
|------|---------|---------------|-----------|------|
| `#bubble` | `max-width: calc(100% - 16px)` | 13×2 + 0.5×2 = 27 | **窗口宽 + 11px** | 仍溢出 11px |
| `#reminder-bar` | `max-width: calc(100% - 16px)` | 14×2 + 0.5×2 = 29 | **窗口宽 + 13px** | 仍溢出 13px |
| `#reminder-panel` | `width: min(250px, calc(100% - 16px))` | 16×2 + 0.5×2 = 33 | 见下 | 小窗口仍溢出 |

`#reminder-panel` 具体算：

- 窗口 288px（默认 0.9x）：`calc` = 272 → `min(250,272)` = 250 → 渲染 **283px**，左右各余 2.5px —— 和修复前**完全一样**
- 窗口 160px（0.5x 下限）：`calc` = 144 → `min(250,144)` = 144 → 渲染 **177px** → **溢出 17px**

**一行修好三个**：

```css
*, *::before, *::after { box-sizing: border-box; }
```

加上之后 `calc(100% - 16px)` 才真的留出左右各 8px 边距。加完建议把 0.5x / 0.9x / 2.0x 三档各截一张图复验。

---

### R-2 · `.gitignore` 仍缺 `release/`

CI 的产物路径已经跟着 `4c9ab48` 改成 `viewer/release/*.exe`（`ci.yml:122`），但 `.gitignore` 至今只有：

```
.DS_Store
__pycache__/
*.pyc
node_modules/
dist/
```

任何人本地跑一次 `npm run dist`，`viewer/release/` 里的几百 MB 安装包就是**未忽略的 untracked 文件**，`git add -A` 一把梭直接进仓库。

顺带建议一并补上：`.venv/`、`.pytest_cache/`、`.idea/`（你本地现在就有一个未忽略的 `.idea/`）。

---

### R-3 · 穿透监听重复绑定

`src/main.ts:166` 和 `:169` 现在同时存在：

```ts
app.canvas.addEventListener('pointermove', updateMouseIgnore)
document.addEventListener('pointermove', updateMouseIgnore, true)
```

`document` 的捕获阶段监听已经覆盖了 canvas 上的所有移动，第 166 行是冗余的——canvas 上每次移动会走两遍 `uiRects()`（含 `querySelectorAll` + 多次 `getBoundingClientRect`，强制布局回流）。功能无害（`lastIgnore` 做了幂等守卫），但可以删掉一行。

---

## A · `pipeline/` 脚本（4 个文件 287 行，第一轮未覆盖）

管线是这个仓库的立身之本，但脚本本身的健壮性远低于文档质量。

### A-1 · `select_frames.py` 会无提示清空 `--output` 目录里的所有文件 ⚠️

`pipeline/select_frames.py:79-81`

```python
os.makedirs(args.output, exist_ok=True)
for f in os.listdir(args.output):
    os.remove(os.path.join(args.output, f))
```

**没有确认、没有 `--force`、README 和 docs 里一个字没提。** 用户把 `--output` 敲成 `~/Documents` 或 `.`，这行会把该目录下**所有文件**删掉（子目录因 `os.remove` 抛 `IsADirectoryError` 而中断，此时前面的文件已经删了）。

这是整个仓库唯一一处不可逆的破坏性操作，而它藏在一个名字听起来完全无害的脚本里。

**修**：只删自己认识的产物（`p_*.png`）；或加 `--clean` 显式开关；或目录非空且含非 `p_*.png` 文件时报错退出。**并写进 07-踩坑实录**——它完全符合那一篇"每条坑都是一条铁律"的体例。

### A-2 · 文件名解析假设：非 `xxx_NNN.png` 命名直接崩，且崩在最后

`select_frames.py:67`

```python
out.sort(key=lambda f: int(f.split('_')[1].split('.')[0]))
```

| 用户的抽帧命名 | 结果 |
|---------------|------|
| `frame_001.png` | ✅ |
| `0001.png`（ffmpeg 常见默认） | `split('_')` 只有 1 段 → **IndexError** |
| `redshao_run_001.png` | `[1]` = `'run'` → `int('run')` → **ValueError** |

而 `--input` 的 help 只写了"抽帧目录（PNG）"，README 的 ffmpeg 步骤也没规定命名。更糟的是这行在**所有帧差异计算跑完之后**才执行——用户等完全程才拿到一个裸 traceback。

**修**：第 30 行已经 `sorted()` 过了，直接按在 `names` 里的下标排即可，彻底去掉文件名解析：

```python
order = {f: i for i, f in enumerate(names)}
out.sort(key=lambda f: order[f])
```

### A-3 · `make_sheet.py` 逐帧归一化缩放 → 动画会"胀缩呼吸"

`pipeline/make_sheet.py:57`

```python
scale = (target - pad * 2) / max(crop.size)
if scale < 1:
    crop = crop.resize(...)
```

`scale` 是**按每一帧自己的尺寸**算的。当帧内容小于 `target`（未被 `cell` 截断时的常见情况），`scale > 1`，被 `if scale < 1` 跳过，相对大小得以保留 —— 这条路径是对的。

但当 `target` 被 `cell` 截断时（`target = min(max_dim + 2*pad, cell)`，即内容大于 1024px 的高清素材），**所有 max 边超过 `target-2pad` 的帧都会各自被缩到恰好 `target-2pad`**：

- "奔跑"帧（伸展、长）→ 缩到 1024-2pad
- "蜷缩"帧（紧凑、短）→ 也缩到 1024-2pad

两帧里的狗**渲染出来一样大**，而它们本该差一截。播放时表现为宠物在帧间莫名胀缩。

这正是 docs/06 §2 用"帧间差异 0.011 / 首尾衔接 0.021"那种精度在追求的东西，却被拼表环节的一个 per-frame 缩放悄悄破坏了。

**修**：算一个**全局** scale（用所有帧的 `max_dim`），所有帧共用：

```python
gscale = min(1.0, (target - pad * 2) / max(max_w, max_h))
```

### A-4 · 全透明输入 → `max([])` 裸崩

`make_sheet.py:40-42`

```python
ws = [b[2] - b[0] for b in boxes if b]
hs = [b[3] - b[1] for b in boxes if b]
max_w, max_h = max(ws), max(hs)      # ← boxes 全为 None 时 ValueError
```

第 48 行有 `if box is None` 的友好警告分支，但它在第 42 行**之后**才执行。"rembg 整批抠图失败"恰恰是最可能的真实故障（模型下载失败、输入不是 RGBA），此时用户拿到的是 `ValueError: max() arg is an empty sequence`，而不是"所有帧都是全透明，请检查抠图结果"。

### A-5 · bbox 的 off-by-one

`alpha_bbox` 返回 `xs.max(), ys.max()`（**闭区间**），而 `img.crop((x0,y0,x1,y1))` 的右下是**开区间**（`make_sheet.py:55`）→ 每帧右边和下边各丢 1 像素。视觉上几乎看不出，但 `ws`/`hs` 因此系统性少 1，`target` 也跟着偏。修法：`return xs.min(), ys.min(), xs.max()+1, ys.max()+1`。

### A-6 · 帧序号只有两位，≥100 帧字典序错乱

`select_frames.py:85` 的 `p_{i:02d}` 和 `remove_bg.py:28` 的 `c_{i:02d}`，配合下游一律 `sorted(os.listdir(...))`：帧数到 100 时 `c_100.png` 会排在 `c_20.png` **前面**，精灵表帧序直接乱。

当前定稿 12 帧，但 docs/06 §2 明确讨论了 24 帧方案，且"帧数×cell ≤ 16384"允许 cell=512 时排到 32 帧。改成 `{i:03d}` 是零成本的。

### A-7 · `remove_bg.py` 不暴露任何抠图参数，与 07 踩坑实录脱节

脚本只有 `--input` / `--output`（`remove_bg.py:20-21`）。而 rembg 真正决定成败的两个旋钮都没有：

- `--model`（`u2net` / `isnet-general-use` / `silueta`…）——不同模型对动物毛发差异极大
- `alpha_matting`（`alpha_matting_foreground_threshold` 等）——**这正是处理毛发边缘的标准手段**

而 docs/07 花了篇幅讲"白毛腿"的抠图边缘问题，docs/05 §工具可替换矩阵也把 rembg 列为可替换项 —— 但脚本本身把用户锁死在默认模型上，遇到边缘问题时**无参可调，只能换工具**。

同时：单帧异常会中断整批（模型推理很慢，20 帧可能几分钟），无跳过、无断点续跑、无进度输出（只在每帧完成后 print）。

### A-8 · 四个脚本共有的健壮性缺口

- `--input` 不存在 → `os.listdir` 裸 `FileNotFoundError`，无友好提示（4 个脚本全部）
- `--frames 0` 或负数 → `select_frames` 第 53 行 `picks[0]` **IndexError**；`--pad 0.5` → `target - pad*2` 可能 ≤ 0
- 所有 `Image.open()` 都不关闭；`select_frames.py:33` 一次性打开**全部**帧并常驻字典 —— 20 帧无所谓，长视频抽帧（几百帧）会撑爆文件句柄
- `make_sheet.py:76` `sheet.save()` **静默覆盖**同名精灵表，无提示

### A-9 · 三个阶段的输出命名各不相同

`select_frames` → `p_00.png`，`remove_bg` → `c_00.png`，`clean_cutout` → **保留原名**。中途出问题时无法把 `c_07.png` 回溯到原始抽帧的哪一帧。建议全程保留原始 stem 或统一加前缀。

---

## B · 测试的有效性（不是覆盖率，是"能不能抓到 bug"）

### B-1 · CI 从不运行 viewer 的单元测试 ⚠️

`.github/workflows/ci.yml` 的 `viewer-build` job 只有三步：`npm ci` → `npm run build` → `test -f dist/index.html`。

**没有 `npm test`。** 全文件 grep `vitest` / `npm test` → 0 命中。

也就是说：`viewer/tests/state-priority.test.ts` 的 9 个用例，以及你刚为提醒解析修复写的 6 个用例（`reminder.test.ts`），**从来没有在 CI 里跑过一次**。`pipeline-test` job 反倒老老实实跑了 smoke + pytest。

一行修好：

```yaml
- name: 单元测试（vitest）
  run: npm test
  working-directory: viewer
```

### B-2 · `npm run build` 从不做类型检查，`tsconfig` 形同虚设

`package.json:10` → `"build": "vite build"`。vite 用 esbuild，**只转译不校验类型**。而 `tsconfig.json` 开了 `"strict": true` + `"noEmit": true` —— 一个纯校验用的配置，却**没有任何脚本会执行它**。

`docs/10-架构升级评估.md:102` 自己承认："未新增 TS 类型错误（**5 个遗留错误**待白天清理）"。这 5 个错误在 CI 里永远是绿的。

另外 `"include": ["src"]` 不含 `tests/`，测试文件也不在类型检查范围内。

**修**：`"build": "tsc --noEmit && vite build"`，`include` 加上 `tests`。

### B-3 · 测试夹具恰好绕开了 A-2 的 bug

`tests/test_pipeline.py:37` 和 `tests/smoke_test.py:32` 造数据都用 `f'f_{i:02d}.png'` —— **正好满足** `select_frames.py:67` 那个 `split('_')[1]` 的隐含假设。

所以 A-2 那个"换个命名就崩"的 bug，**现有测试在设计上就不可能抓到**。这是测试有效性的典型陷阱：夹具是照着实现写的，于是把实现的假设一起固化进了测试。

**修**：加一个用 `0001.png` / `my_pet_run_01.png` 命名的参数化用例——它现在必然红，修完 A-2 才会绿。

### B-4 · `main()` 与 `remove_bg.py` 零覆盖

- 4 个脚本的 `main()` 都没有单测（smoke_test 通过 subprocess 覆盖了 3 个的快乐路径），**包括 A-1 那个危险的清空逻辑**
- `remove_bg.py` 完全没有测试。它依赖模型下载不好测，但可以 monkeypatch `rembg.remove` 来测文件遍历、命名、错误处理这些真正会出问题的部分
- 未覆盖的边界：全透明输入（A-4）、`--frames 0`（A-8）、≥100 帧排序（A-6）

---

## C · 文档

### C-1 · docs/06 的标题承诺与覆盖范围不符

文档标题是「参数详解：**每个数字背后是实验**」，开篇结论是「**所有默认值**都有实验数据支撑，不是拍脑袋」。

但它只讲了 `pet.json` 的 8 个运行时参数。**管线脚本的参数一个都没讲**：

| 参数 | 默认值 | 文档里有吗 |
|------|--------|-----------|
| `select_frames --min-diff` | 0.02 | ✗（docs/05:90 只有一句"差异度阈值是经验值，按自己宠物的动作幅度微调"，没说是哪个参数、默认多少、往哪个方向调） |
| `select_frames --frames` 与 `pet.json` 的 `frames` 关系 | — | ✗（两者必须一致，否则精灵表帧数与配置对不上） |
| `make_sheet --pad` | 0.06 | ✗（完全没出现过） |
| `make_sheet --cell` | 1024 | △（只在"帧数×cell ≤ 16384"铁律里出现，没讲调它的取舍） |
| `clean_cutout --min-size` | 500 | △（docs/05、07 提到"<500px"，但没讲它是**绝对像素**——同一个 500 在 256px 帧和 4K 帧上是完全不同的力度） |
| `alpha > 10` 的阈值（出现在两个脚本里） | 10 | ✗ |

README 的「快速开始」四条命令**全是管线脚本**。用户第一个想调的就是这些数，而"每个数字背后是实验"的那篇文档里没有它们。

**修**：docs/06 加一节「管线参数」，或新开一篇。哪怕只写"这个值是经验值，没做过实验"也比不写强——诚实标注是这个仓库已经在用的风格（docs/09 就是范例）。

### C-2 · 全仓库指向 `.md` 的链接数 = 0

11 篇文档 + README，**没有任何一条 markdown 链接指向另一篇文档**。README 全文只有 2 个链接（`assets/demo.gif` 和 `LICENSE`）。

所有交叉引用都是纯文本：`见 07`、`见 05 §3`、`见 01 §6.2`、`见 08-接口协议.md`（出现 20+ 次）。README 的「快速上手三条路径」表格里 `01（数据模型）→ 02（提示词）→ 05（素材管线）` 也全是纯文本，「文档导航」整块在代码块里。

在 GitHub 上读者**一个都点不动**，只能滚回目录再手动找文件。对一个核心价值就是"方法论文档"的仓库，这是最低成本、最高收益的一处改进。

**修**：脚本批量替换 `见 0X` → `见 [0X-标题](docs/0X-标题.md)`，README 导航块从代码块改成列表 + 链接。

---

## D · 安全面（宠物包按设计是要分享的，所以这不是理论）

docs/08 定义了宠物包格式，README 也在推动"把 examples/redshao/ 整理成宠物包"——即**宠物包会在用户之间传播**。那么一个恶意宠物包能做什么，就是真实攻击面。

### D-1 · `pet:load` 是唯一没做 `safePetId` 校验的 handler

`viewer/main.js:156-163` 直接 `path.join(PETS_ROOT, petId, 'pet.json')`，而同文件的 `pet:activity:list`、`pet:theme`、`appendActivity` 都调了 `safePetId()`（`main.js:232`）。校验不一致本身就是隐患。

### D-2 · `pet:file` 的路径边界检查用前缀匹配，可绕过

`viewer/main.js:189-192`

```js
const full = path.join(PETS_ROOT, petId, relPath)
if (!full.startsWith(PETS_ROOT)) return { error: 'invalid path' }
```

`startsWith` 是**字符串前缀**判断，不是路径边界判断：`~/.petpet/pets-evil/secret.png` 的前缀是 `~/.petpet/pets`，**校验通过**。

标准写法：

```js
const rel = path.relative(PETS_ROOT, full)
if (rel.startsWith('..') || path.isAbsolute(rel)) return { error: 'invalid path' }
```

### D-3 · CSP 的 `connect-src` 允许连接**任意** websocket 主机

`viewer/index.html:5`

```
connect-src 'self' data: blob: ws: http://127.0.0.1:5173
```

`ws:` 是**无主机限制的 scheme-source**，匹配任何 `ws://` 地址。而这份 CSP 在生产构建里原样保留（vite 直接拿 `index.html` 当模板），连 dev server 的 `http://127.0.0.1:5173` 也留在了发布版里。

D-2 + D-3 组合起来就是一条完整链路：恶意宠物包的 `pet.json` 里写 `"file": "../../../../../etc/passwd"` → 读成 dataURL → 通过任意 websocket 外传。

**修**：`ws:` 收窄为 `ws://127.0.0.1:5173`，并且只在 dev 模式注入（vite 的 `transformIndexHtml` 或直接分两份 CSP）。

### D-4 · 两个窗口都 `sandbox: false`

`main.js:38` 与日记窗口（`main.js:327`）。`contextIsolation: true` + `nodeIntegration: false` 已经是对的，但 `sandbox: false` 让 preload 保有完整 Node 能力。当前 preload 很小、暴露面克制，风险不高，属于**加固项**：改成 `sandbox: true` 后 preload 仍可用 `contextBridge`/`ipcRenderer`，值得试一次。

---

## E · 仓库工程化

### E-1 · 无任何 lint / format 配置

没有 eslint、prettier、ruff、flake8、`.editorconfig`、pre-commit。对一个明确欢迎他人贡献的方法论仓库，这意味着每个 PR 的代码风格都得靠人肉 review。

Python 侧建议 `ruff`（一个工具覆盖 lint + format，零配置起步）；前端侧 `eslint` + `@typescript-eslint`。

### E-2 · 依赖全部无上界，且无 lockfile

`requirements.txt` 四个依赖全是 `>=`，没有上界、没有 `requirements.lock`／`pip-compile` 产物，CI 也不缓存锁定版本：

```
numpy>=1.24   Pillow>=10.0   rembg>=2.0   scipy>=1.10
```

numpy 2.0、Pillow 11、scipy 的新版都发生过 API 移除。具体值得核对一处：`clean_cutout.py:30` 用的 `scipy.ndimage.sum`，新版 SciPy 已推荐改用 `sum_labels`——建议确认当前 scipy 版本下是否已产生 DeprecationWarning。

CI 只跑 Python 3.11（`ci.yml:20`），而你本机是 3.14 —— **rembg 依赖 onnxruntime，在 3.14 上大概率装不上**。requirements.txt 头部写的是"Python 3.9+"，建议改成经过验证的区间（如 3.9–3.12）并在 CI 里跑矩阵。

### E-3 · CI 不覆盖唯一被实测过的平台

三个 job 是 `ubuntu` × 2 + `windows` × 1。**没有 macOS**——而 README 和 docs/09 反复强调"macOS 已实测"，`main.js` 里还有一整块 `process.platform === 'darwin'` 的 Dock 图标逻辑、`setVisibleOnAllWorkspaces`、`'screen-saver'` 层级这些 macOS 专属路径。

唯一被宣称支持的平台，在 CI 里一次都不构建。加一个 `macos-latest` 的 build job 成本极低。

### E-4 · 授权边界：MIT 覆盖不了 `examples/` 里的真实照片

`LICENSE` 是标准 MIT（`Copyright (c) 2026 stshourenxy-dev`），但仓库里有 4.6MB 的 `examples/`：红苕和包包的**真实宠物照片**、AI 生成的皮克斯风设定图，加上 1MB 的 `assets/demo.gif`。

MIT 是软件许可证，用在照片/生成素材上语义不清（"允许任何人自由使用、修改、再分发这些照片"是不是你的本意？）。方法论类仓库的常见做法是分层声明：

- 代码（`pipeline/`、`viewer/`、`tests/`）→ MIT
- 文档（`docs/`、README）→ CC BY 4.0 或 CC BY-SA
- 素材（`examples/`、`assets/`）→ 单独声明（如"仅供参考，不授权再分发"）

同时建议在 README 或 `examples/README.md` 里明确 AI 生成素材的平台条款归属（即梦/可灵的生成物权利归属，这对下游用户是真实的合规问题）。

### E-5 · 缺社区文件

无 `CONTRIBUTING.md`、无 issue 模板、无 PR 模板、无 `CODE_OF_CONDUCT.md`（`.github/` 下只有 `workflows/`）。

对这个仓库尤其可惜的是：`docs/审查清单.md` 刚刚沉淀了一套很成体系的对抗性审查方法——它天然就是 `CONTRIBUTING.md` 里"提 PR 前自查"那一节的内容。另外提交信息的 Conventional Commits + 中文正文规范，目前只能靠新贡献者去翻 git log 反推。

---

## 建议的落地顺序

**一行/几行就能修，且收益最高**
1. R-2 `.gitignore` 补 `release/`（防止有人误提交几百 MB）
2. B-1 CI 加 `npm test`（你刚写的 6 个提醒测试目前根本没跑）
3. R-1 全局 `box-sizing: border-box`（一行修好三个浮层溢出）
4. A-2 `select_frames` 去掉文件名解析（换个下标排序）

**安全 + 正确性**

5. D-2 路径边界改 `path.relative`；D-1 补 `safePetId`；D-3 CSP 收窄 `ws:`
6. A-1 危险清空加保护 + 写进 07-踩坑实录
7. A-3 全局 scale（这个直接影响动画质量，属于 docs/06 关心的那类精度）
8. A-4 全透明输入的友好报错

**质量基建**

9. B-2 `build` 加 `tsc --noEmit`（顺带清掉 docs/10 承认的 5 个遗留错误）
10. B-3 加一个"非标准命名"的参数化用例
11. E-1 引入 ruff + eslint；E-2 依赖加上界

**文档与社区（对外价值最大）**

12. C-2 全仓库补 `.md` 互链
13. C-1 docs/06 补管线参数一节
14. E-4 授权分层；E-5 `CONTRIBUTING.md`（把 `docs/审查清单.md` 接进去）
15. E-3 CI 加 macOS build

---

## 附：本轮验证方法

- **复验**：`git diff 1d49fa3..HEAD` 逐文件核对，不采信提交信息，只看代码。R-1 的溢出量按 CSS content-box 规范手算（`max-width` 约束内容盒，padding/border 外加），已确认全仓库唯一的 `box-sizing` 声明在 `#reminder-input` 且早于本轮修复。
- **CI 覆盖**：全文件 grep `npm test` / `vitest` → 0 命中，据此判定 viewer 单测从未在 CI 执行。
- **链接统计**：Python 正则解析 README + `docs/*.md` 全部 12 个文件的 `[..](..)`，区分图片与链接，指向 `.md` 的链接数为 0。
- **管线脚本**：源码走查，未实跑（本机 Python 3.14 装不上 rembg，见 E-2）。A-2 / A-4 / A-8 的崩溃路径由代码直接可推；**A-3 的动画胀缩效果建议实跑一组超过 `cell` 的高清素材复验**。
- **安全面**：静态审查 IPC handler 与 CSP，未构造实际的恶意宠物包验证。
